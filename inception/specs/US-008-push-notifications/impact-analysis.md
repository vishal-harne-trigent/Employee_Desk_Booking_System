# US-008 — impact analysis

**Surfaces:** Persistence (`NotificationPreferences`), Dependency (WebPush), Contract (API + push subscription JSON).

**Regression:** Email flow unchanged; push is additive and opt-in only.
